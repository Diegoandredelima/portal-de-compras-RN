# Arquivos de texto que devem ser normalizados*.html text*.css text*.js text*.md text*.txt text*.json text# Arquivos binários que não devem ser modificados*.png binary*.jpg binary*.jpeg binary*.gif binary*.ico binary*.pdf binary*.zip binary*.tar binary*.gz binary# Arquivos que devem ser ignorados pelo Git LFS*.png !filter=lfs*.jpg !filter=lfs*.jpeg !filter=lfs*.gif !filter=lfs*.ico !filter=lfs*.pdf !filter=lfs*.zip !filter=lfs*.tar !filter=lfs*.gz !filter=lfs# Arquivos que devem ser executáveis*.sh text eol=lf*.bat text eol=crlf*.cmd text eol=crlf# Arquivos que devem ser mantidos com LF*.html text eol=lf*.css text eol=lf*.js text eol=lf*.md text eol=lf*.txt text eol=lf*.json text eol=lf# Arquivos que devem ser mantidos com CRLF*.bat text eol=crlf*.cmd text eol=crlf*.ps1 text eol=crlf
